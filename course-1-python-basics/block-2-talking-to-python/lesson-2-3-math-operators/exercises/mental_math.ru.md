# Устный счёт — Урок 2.3

> [English →](mental_math.md)

Предскажи результат **до** запуска Python. Запиши догадку, потом проверь маленьким скриптом или калькулятором.

---

## Раунд 1: Чистые числа

| Выражение | Твой прогноз | Факт |
|-----------|--------------|------|
| `10 + 3` | | |
| `10 - 3` | | |
| `10 * 3` | | |
| `10 / 3` | | |

**Подсказка:** `/` всегда даёт десятичное число, даже если ответ целый.

---

## Раунд 2: Переменные

```python
a = 8
b = 2
```

| Выражение | Твой прогноз | Факт |
|-----------|--------------|------|
| `a + b` | | |
| `a - b` | | |
| `a * b` | | |
| `a / b` | | |

---

## Раунд 3: int() с input

Если ввести `5` и `4`:

```python
num1 = int(input("First: "))   # вводишь 5
num2 = int(input("Second: "))  # вводишь 4
print(num1 + num2)
```

Что напечатается? _______

---

## Дополнительно

Что будет, если ввести `hello` вместо числа? Запусти [starter/calculator.py](../starter/calculator.py) и попробуй — прочитай сообщение `ValueError`.

[← Урок 2.3](../README.md)
