# Урок 1.3: Фигуры и координаты

> **Курс:** Разработка игр на Python · **Блок:** Знакомство с Pygame · **~30–35 мин**  
> [Выбрать язык](README.md) · [English →](en.md)

<!-- meta
homework: starter/game.py
minutes: 30
-->

---

## Title

**Уровень 3 — Капитан координат**

---

## Объяснение

Пора поставить **героя на карту**. В Pygame карта — сетка пикселей. Важный сюрприз по сравнению с уроками математики:

| Идея | В Pygame |
|------|----------|
| Начало `(0, 0)` | **Левый верхний** угол окна |
| Больше `x` | Движение **вправо** |
| Больше `y` | Движение **вниз** (не вверх!) |

Прямоугольнику нужны левый верхний угол `(x, y)` плюс ширина и высота:

```python
pygame.draw.rect(screen, (255, 200, 50), (x, y, box_w, box_h))
```

Двигай героя, меняя переменные каждый кадр:

```python
x = x + speed
```

Когда `x` уйдёт за правый край — верни его слева, чтобы путешествие не кончалось.

---

### Шаг 1: Открой starter/game.py

Открой [starter/game.py](starter/game.py). Окно, заливка и выход готовы. TODO: **движение** и **рисование**.

---

### Шаг 2: Обновляй x каждый кадр

После цикла событий, до отрисовки:

```python
x = x + speed
```

---

### Шаг 3: Нарисуй прямоугольник

После `screen.fill(...)`:

```python
pygame.draw.rect(screen, (255, 200, 50), (x, y, box_w, box_h))
```

Цвет золотистый; можно сменить на неоновый.

---

### Шаг 4: Перенос (wrap) — есть в решении

```python
if x > WIDTH:
    x = -box_w
```

Старт с `-box_w` позволяет коробке въехать слева.

---

### Шаг 5: Запуск

```text
cd course-3-game-dev\block-1-getting-started-pygame\lesson-1-3-shapes-coordinates
python starter\game.py
```

**Mac/Linux:** `python starter/game.py`

**Ожидаемый результат (визуально):** Тёмный фон; золотая коробка едет вправо; с wrap снова появляется слева. Выход крестиком.

---

## Пример кода

**Файл: [solution/game.py](solution/game.py)**

```python
import pygame

pygame.init()

WIDTH = 640
HEIGHT = 480
screen = pygame.display.set_mode((WIDTH, HEIGHT))
pygame.display.set_caption("Box Voyager")

clock = pygame.time.Clock()
running = True

x = 100
y = 200
box_w = 50
box_h = 50
speed = 3

while running:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False

    x = x + speed
    if x > WIDTH:
        x = -box_w

    screen.fill((20, 24, 40))
    pygame.draw.rect(screen, (255, 200, 50), (x, y, box_w, box_h))

    pygame.display.flip()
    clock.tick(60)

pygame.quit()
```

---

## Запуск кода

```text
cd course-3-game-dev\block-1-getting-started-pygame\lesson-1-3-shapes-coordinates
python starter\game.py
```

**Ожидаемый результат (визуально):** Движущийся залитый прямоугольник на тёмном фоне; перенос за правым краем (после wrap или по решению).

---

## Быстрые упражнения

1. **Турбо** — `speed = 10`. Всё ещё читаемо?
2. **Вертикальный рейс** — двигай `y` вместо `x`; wrap по `HEIGHT`.
3. **Маленький скаут** — `box_w = 20`, `box_h = 20`.

---

## Задание для практики

**Квест:** Капитан координат

1. Закрой TODO в [starter/game.py](starter/game.py): обнови `x`, нарисуй rect.
2. Добавь wrap, чтобы коробка возвращалась слева (см. Пример кода).
3. **Бонус:** Отскок вместо wrap — у края меняй `speed` на `-speed`.

**Самопроверка:** Коробка видна? Движется? Wrap или отскок? ✓

**Эталонное решение:** [solution/game.py](solution/game.py)

---

## Уголок отладки

**Проблема:** Вызываешь `draw.rect`, но коробки не видно — только фон.

**Причина:** Рисование **до** `fill` (заливка закрашивает коробку), или `x`/`y` далеко за экраном, или забыт `flip()`.

**Исправление:** Порядок кадра: события → обновить `x`/`y` → `fill` → `draw.rect` → `flip` → `tick`. Проверь, что `x` около `100`, `y` около `200`.

---

## Проверь себя

Выбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!

1. Где находится `(0, 0)` в окне Pygame?
   - **a)** Слева снизу (как на уроке математики)
   - **b)** Слева сверху
   - **c)** В центре экрана
   - **d)** Справа снизу

2. Что делает увеличение `y`?
   - **a)** Двигает фигуру вверх
   - **b)** Двигает фигуру вниз
   - **c)** Меняет заголовок окна
   - **d)** Устанавливает Pygame

3. Какой вызов рисует залитый прямоугольник?
   - **a)** `pygame.draw.rect(screen, color, (x, y, w, h))`
   - **b)** `screen.fill(rect)`
   - **c)** `turtle.forward(w)`
   - **d)** `pip install rect`

4. Зачем обновлять `x` каждый кадр?
   - **a)** Чтобы закрыть окно
   - **b)** Чтобы позиция коробки менялась со временем (движение)
   - **c)** Чтобы навсегда сменить RGB
   - **d)** Чтобы создать venv

5. Хороший порядок одного кадра:
   - **a)** flip → fill → draw → update
   - **b)** события → обновить позицию → fill → draw → flip → tick
   - **c)** quit → init → pip
   - **d)** рисовать только один раз до цикла

---

<details><summary>Нажми, чтобы увидеть ответы</summary>

1. **b)** Начало координат Pygame — левый верхний пиксель.
2. **b)** Больше `y` — ниже на экране.
3. **a)** `draw.rect` принимает поверхность, цвет и прямоугольник `(x, y, w, h)`.
4. **b)** Смена `x` каждый кадр и создаёт движение.
5. **b)** Обнови, нарисуй, покажи, затем задай темп кадра.

</details>

---

## Что дальше

→ [Урок 1.4: Дельта-время и плавное движение](../lesson-1-4-delta-time-smooth-movement/README.md) — тот же рейс, но плавно на быстром и медленном ПК.

---

*Коробка плывёт. Дальше — научим её уважать настоящее время!*

[← Выбрать язык](README.md)
