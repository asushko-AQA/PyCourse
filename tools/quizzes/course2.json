{
  "lesson-0-1-virtual-environments": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. What is a virtual environment (`.venv`) like in this lesson?\n   - **a)** A folder that keeps packages for just this project\n   - **b)** A special browser for testing websites\n   - **c)** A backup copy of all your Python files\n   - **d)** A command that deletes old packages\n\n2. When your venv is active, what do you often see at the start of the terminal prompt?\n   - **a)** `[Flask]`\n   - **b)** `(.venv)`\n   - **c)** `>>> `\n   - **d)** `C:\\Windows\\`\n\n3. What does `pip install flask` do in an **activated** venv?\n   - **a)** Installs Flask only inside that venv\n   - **b)** Deletes Flask from your computer\n   - **c)** Opens Flask in the browser automatically\n   - **d)** Copies Flask into every Python project on your PC\n\n4. Why should you activate the venv before running `python starter\\check_setup.py`?\n   - **a)** So Python can find Flask that you installed in the venv\n   - **b)** Because `.py` files only run after activation\n   - **c)** To make the script run twice as fast\n   - **d)** Because VS Code refuses to open the terminal otherwise\n\n5. What does `deactivate` do?\n   - **a)** Turns off the venv and returns you to normal terminal Python\n   - **b)** Deletes the `.venv` folder forever\n   - **c)** Uninstalls Flask from the internet\n   - **d)** Closes VS Code\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** A venv is a project toolbox — packages stay separate from other projects.\n2. **b)** `(.venv)` in the prompt means the virtual environment is active.\n3. **a)** `pip install` inside an active venv installs only there.\n4. **a)** Without activation, Python may not see packages you installed in `.venv`.\n5. **a)** `deactivate` exits the venv — you are back to system Python.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. На что похоже **виртуальное окружение** (`.venv`) в этом уроке?\n   - **a)** Папка, где пакеты только для этого проекта\n   - **b)** Особый браузер для проверки сайтов\n   - **c)** Резервная копия всех твоих Python-файлов\n   - **d)** Команда, которая удаляет старые пакеты\n\n2. Когда venv **активен**, что часто видно в начале строки терминала?\n   - **a)** `[Flask]`\n   - **b)** `(.venv)`\n   - **c)** `>>> `\n   - **d)** `C:\\Windows\\`\n\n3. Что делает `pip install flask` в **активированном** venv?\n   - **a)** Ставит Flask только внутрь этого venv\n   - **b)** Удаляет Flask с компьютера\n   - **c)** Сам открывает Flask в браузере\n   - **d)** Копирует Flask во все Python-проекты на ПК\n\n4. Зачем активировать venv перед `python starter\\check_setup.py`?\n   - **a)** Чтобы Python нашёл Flask, установленный в venv\n   - **b)** Потому что `.py` запускаются только после активации\n   - **c)** Чтобы скрипт работал в два раза быстрее\n   - **d)** Потому что VS Code иначе не откроет терминал\n\n5. Что делает `deactivate`?\n   - **a)** Выключает venv и возвращает обычный Python в терминале\n   - **b)** Навсегда удаляет папку `.venv`\n   - **c)** Удаляет Flask из интернета\n   - **d)** Закрывает VS Code\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** venv — ящик инструментов проекта; пакеты отделены от других проектов.\n2. **b)** `(.venv)` в приглашении значит, что окружение активно.\n3. **a)** `pip install` в активном venv ставит пакеты только туда.\n4. **a)** Без активации Python может не видеть пакеты из `.venv`.\n5. **a)** `deactivate` выходит из venv — снова системный Python.\n\n</details>\n\n---"
  },
  "lesson-0-2-how-the-web-works": {
    "en": "## Quick Check\n\n*Concept check — you also built the coding quiz in `web_quiz.py`. These questions test the ideas.*\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. In the browser–server story, who **sends the request** first?\n   - **a)** Browser\n   - **b)** Server\n   - **c)** URL\n   - **d)** pip\n\n2. What is a **URL**?\n   - **a)** The web address that tells the browser where to ask\n   - **b)** A Python file that runs Flask\n   - **c)** The HTML code inside a page\n   - **d)** A kind of virtual environment\n\n3. What does a **server** do in this lesson?\n   - **a)** Waits for requests and sends back responses\n   - **b)** Only draws pictures on your screen\n   - **c)** Stores all your `.venv` folders\n   - **d)** Replaces the need for a browser\n\n4. In the round trip, what is a **response**?\n   - **a)** What the server sends back (like an HTML page)\n   - **b)** What you type into `input()` in the quiz\n   - **c)** The name of the Flask install command\n   - **d)** A broken link in the address bar\n\n5. What does status **200 OK** usually mean?\n   - **a)** Success — here is the page you asked for\n   - **b)** The browser is offline\n   - **c)** The server deleted the website\n   - **d)** You must install Flask again\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** The browser asks first; the server answers.\n2. **a)** URL = the address bar text that points to a resource.\n3. **a)** The server listens and replies with pages or data.\n4. **a)** A response is what travels back from server to browser.\n5. **a)** 200 OK means the request succeeded.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\n*Проверка понятий — кодовый квиз ты также собираешь в `web_quiz.py`. Здесь проверяем идеи.*\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. В цепочке браузер–сервер кто **первым отправляет запрос**?\n   - **a)** Браузер\n   - **b)** Сервер\n   - **c)** URL\n   - **d)** pip\n\n2. Что такое **URL**?\n   - **a)** Веб-адрес, который говорит браузеру, куда обращаться\n   - **b)** Python-файл, который запускает Flask\n   - **c)** HTML-код внутри страницы\n   - **d)** Вид виртуального окружения\n\n3. Что делает **сервер** в этом уроке?\n   - **a)** Ждёт запросы и отправляет ответы\n   - **b)** Только рисует картинки на экране\n   - **c)** Хранит все твои папки `.venv`\n   - **d)** Заменяет браузер\n\n4. В полном цикле что такое **ответ (response)**?\n   - **a)** То, что сервер отправляет обратно (например HTML-страницу)\n   - **b)** То, что ты вводишь в `input()` в квизе\n   - **c)** Название команды установки Flask\n   - **d)** Битая ссылка в адресной строке\n\n5. Что обычно значит статус **200 OK**?\n   - **a)** Успех — вот страница, которую ты запросил\n   - **b)** Браузер без интернета\n   - **c)** Сервер удалил сайт\n   - **d)** Нужно снова установить Flask\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Сначала спрашивает браузер; отвечает сервер.\n2. **a)** URL — текст в адресной строке, указывающий на ресурс.\n3. **a)** Сервер слушает и отвечает страницами или данными.\n4. **a)** Ответ — то, что идёт обратно от сервера к браузеру.\n5. **a)** 200 OK значит, что запрос прошёл успешно.\n\n</details>\n\n---"
  },
  "lesson-1-1-installing-flask": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. This lesson is mainly a check that Flask is ready — not a repeat of what step from Block 0?\n   - **a)** Creating and using a virtual environment\n   - **b)** Learning Turtle graphics\n   - **c)** Writing Mad-Libs in the terminal\n   - **d)** Installing Pygame\n\n2. If `pip list` shows Flask while `(.venv)` is active, where is Flask installed?\n   - **a)** Inside your project's venv\n   - **b)** Only on the Flask company website\n   - **c)** Inside your browser cache\n   - **d)** In every folder on your Desktop\n\n3. You run `python starter\\check_flask.py` and get `ModuleNotFoundError: No module named 'flask'`. What is the most likely fix?\n   - **a)** Activate the venv, then `pip install flask`, then run again\n   - **b)** Delete `.venv` and never use pip again\n   - **c)** Change the port to 8080\n   - **d)** Add `@app.route` to the check script\n\n4. What should `check_flask.py` print when everything works?\n   - **a)** `Flask import OK!` and a Flask version line\n   - **b)** `Hello, Web World!` in the browser only\n   - **c)** A list of every website on the internet\n   - **d)** Nothing — silence means success\n\n5. Why do Flask lessons start by activating `.venv`?\n   - **a)** So Python uses the toolbox where Flask lives\n   - **b)** Because Flask only works on Tuesdays\n   - **c)** To turn on `debug=True` automatically\n   - **d)** Because routes cannot be created without it\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** Block 0 taught venv + pip; this lesson verifies Flask is there.\n2. **a)** Active venv + `pip list` shows packages in that environment.\n3. **a)** Usually the venv is off or Flask was never installed there.\n4. **a)** The check script confirms import and prints the version.\n5. **a)** Flask must be importable from the activated project venv.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Этот урок в основном проверяет готовность Flask — а не повтор какого шага из Блока 0?\n   - **a)** Создание и использование виртуального окружения\n   - **b)** Графика Turtle\n   - **c)** Mad-Libs в терминале\n   - **d)** Установка Pygame\n\n2. Если при активном `(.venv)` в `pip list` есть Flask — где он установлен?\n   - **a)** Внутри venv этого проекта\n   - **b)** Только на сайте компании Flask\n   - **c)** В кэше браузера\n   - **d)** В каждой папке на рабочем столе\n\n3. `python starter\\check_flask.py` даёт `ModuleNotFoundError: No module named 'flask'`. Самое вероятное решение?\n   - **a)** Активировать venv, `pip install flask`, запустить снова\n   - **b)** Удалить `.venv` и больше не использовать pip\n   - **c)** Сменить порт на 8080\n   - **d)** Добавить `@app.route` в проверочный скрипт\n\n4. Что должен вывести `check_flask.py` при успехе?\n   - **a)** `Flask import OK!` и строку с версией Flask\n   - **b)** Только `Hello, Web World!` в браузере\n   - **c)** Список всех сайтов интернета\n   - **d)** Ничего — тишина значит успех\n\n5. Зачем в уроках Flask сначала активируют `.venv`?\n   - **a)** Чтобы Python использовал ящик, где лежит Flask\n   - **b)** Потому что Flask работает только по вторникам\n   - **c)** Чтобы автоматически включить `debug=True`\n   - **d)** Потому что без этого нельзя создать маршруты\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Блок 0 учил venv + pip; этот урок проверяет, что Flask на месте.\n2. **a)** Активный venv + `pip list` показывает пакеты в этом окружении.\n3. **a)** Обычно venv не активен или Flask туда не ставили.\n4. **a)** Проверочный скрипт подтверждает импорт и печатает версию.\n5. **a)** Flask должен импортироваться из активного venv проекта.\n\n</details>\n\n---"
  },
  "lesson-1-2-first-web-page": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. In Flask, what does a **route** connect?\n   - **a)** A URL path to a Python function\n   - **b)** A CSS file to a database\n   - **c)** Two different virtual environments\n   - **d)** The browser to the internet provider only\n\n2. What does `@app.route(\"/\")` mean?\n   - **a)** When someone visits the home path, run this function\n   - **b)** Delete all other routes\n   - **c)** Install Flask automatically\n   - **d)** Open VS Code settings\n\n3. In `http://127.0.0.1:5000`, what does `127.0.0.1` mean?\n   - **a)** This computer (localhost)\n   - **b)** A website in another country\n   - **c)** The Flask version number\n   - **d)** Your Wi-Fi password\n\n4. Why must you keep the terminal open while testing in the browser?\n   - **a)** The Flask server is running there and waiting for requests\n   - **b)** The browser cannot load pages without VS Code open\n   - **c)** Python deletes `app.py` if you close the terminal\n   - **d)** `debug=True` only works with two terminals\n\n5. How do you **stop** the Flask server when you are done?\n   - **a)** Press **Ctrl+C** in the terminal\n   - **b)** Close the browser tab only\n   - **c)** Run `pip uninstall flask`\n   - **d)** Delete the `templates` folder\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** A route maps a URL to the function that builds the response.\n2. **a)** `/` is the home page path.\n3. **a)** `127.0.0.1` is localhost — your own machine.\n4. **a)** Closing the terminal stops the server process.\n5. **a)** **Ctrl+C** stops the running Flask app in the terminal.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Во Flask что связывает **маршрут (route)**?\n   - **a)** Путь URL с функцией Python\n   - **b)** CSS-файл с базой данных\n   - **c)** Два разных виртуальных окружения\n   - **d)** Только браузер с провайдером интернета\n\n2. Что означает `@app.route(\"/\")`?\n   - **a)** При заходе на главный путь вызывается эта функция\n   - **b)** Удаляются все остальные маршруты\n   - **c)** Flask устанавливается автоматически\n   - **d)** Открываются настройки VS Code\n\n3. В `http://127.0.0.1:5000` что значит `127.0.0.1`?\n   - **a)** Этот компьютер (localhost)\n   - **b)** Сайт в другой стране\n   - **c)** Номер версии Flask\n   - **d)** Пароль от Wi-Fi\n\n4. Зачем держать терминал открытым при проверке в браузере?\n   - **a)** Там работает сервер Flask и ждёт запросы\n   - **b)** Браузер не грузит страницы без VS Code\n   - **c)** Python удалит `app.py`, если закрыть терминал\n   - **d)** `debug=True` работает только с двумя терминалами\n\n5. Как **остановить** сервер Flask?\n   - **a)** **Ctrl+C** в терминале\n   - **b)** Только закрыть вкладку браузера\n   - **c)** Выполнить `pip uninstall flask`\n   - **d)** Удалить папку `templates`\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Маршрут связывает URL с функцией, которая строит ответ.\n2. **a)** `/` — путь главной страницы.\n3. **a)** `127.0.0.1` — localhost, твой компьютер.\n4. **a)** Закрытие терминала останавливает процесс сервера.\n5. **a)** **Ctrl+C** останавливает запущенное Flask-приложение.\n\n</details>\n\n---"
  },
  "lesson-1-3-dynamic-routes": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. What makes `/hello/<name>` a **dynamic** route?\n   - **a)** Part of the URL changes and Flask passes it into your function\n   - **b)** The page reloads automatically every second\n   - **c)** It only works on mobile phones\n   - **d)** It requires a CSS file\n\n2. In `@app.route(\"/hello/<name>\")` and `def hello(name):`, what must match?\n   - **a)** The variable name in the path and the function parameter\n   - **b)** The port number and the file name\n   - **c)** The browser brand and the OS version\n   - **d)** The HTML tag and the pip version\n\n3. You visit `/hello/Alex`. What should the `name` argument be?\n   - **a)** `Alex`\n   - **b)** `/hello`\n   - **c)** `name`\n   - **d)** `5000`\n\n4. You visit `/hello/` with nothing after the slash. What often happens?\n   - **a)** Flask cannot match the route — often a 404 error\n   - **b)** The server installs Flask again\n   - **c)** The browser deletes the URL\n   - **d)** Python opens a new `.venv`\n\n5. Why use an f-string like `f\"Hello, {name}!\"`?\n   - **a)** To plug the URL value into the text you send back\n   - **b)** To store files in the `static/` folder\n   - **c)** To activate the virtual environment\n   - **d)** To hide the URL from the user forever\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** `<name>` captures the changing part of the URL.\n2. **a)** The path variable and parameter must share the same name.\n3. **a)** Flask passes the URL segment as `name='Alex'`.\n4. **a)** An empty name usually does not match the route pattern.\n5. **a)** f-strings insert the dynamic value into your greeting.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Почему `/hello/<name>` — **динамический** маршрут?\n   - **a)** Часть URL меняется, и Flask передаёт её в функцию\n   - **b)** Страница сама перезагружается каждую секунду\n   - **c)** Работает только на телефонах\n   - **d)** Требует CSS-файл\n\n2. В `@app.route(\"/hello/<name>\")` и `def hello(name):` что должно совпадать?\n   - **a)** Имя переменной в пути и параметр функции\n   - **b)** Номер порта и имя файла\n   - **c)** Бренд браузера и версия ОС\n   - **d)** HTML-тег и версия pip\n\n3. Ты открываешь `/hello/Alex`. Чем должен быть аргумент `name`?\n   - **a)** `Alex`\n   - **b)** `/hello`\n   - **c)** `name`\n   - **d)** `5000`\n\n4. Заход на `/hello/` без имени после слэша. Что часто происходит?\n   - **a)** Flask не находит маршрут — часто ошибка 404\n   - **b)** Сервер снова устанавливает Flask\n   - **c)** Браузер удаляет URL\n   - **d)** Python открывает новый `.venv`\n\n5. Зачем f-строка `f\"Hello, {name}!\"`?\n   - **a)** Вставить значение из URL в текст ответа\n   - **b)** Хранить файлы в `static/`\n   - **c)** Активировать виртуальное окружение\n   - **d)** Навсегда скрыть URL от пользователя\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** `<name>` захватывает меняющуюся часть URL.\n2. **a)** Имя в пути и параметр функции должны совпадать.\n3. **a)** Flask передаёт сегмент URL как `name='Alex'`.\n4. **a)** Пустое имя обычно не подходит под шаблон маршрута.\n5. **a)** f-строки вставляют динамическое значение в приветствие.\n\n</details>\n\n---"
  },
  "lesson-1-4-multiple-routes": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. How can one Flask `app` serve Home, About, and Jokes?\n   - **a)** Several `@app.route` decorators — one function per path\n   - **b)** Only one route is allowed per file\n   - **c)** You must run a separate `python` command for each page\n   - **d)** Each page needs its own `.venv`\n\n2. In `<a href='/about'>About</a>`, what does `href` tell the browser?\n   - **a)** Which path to request when the link is clicked\n   - **b)** Which CSS color to use\n   - **c)** Which pip package to install\n   - **d)** The secret key for flash messages\n\n3. What does `<int:a>` in `/add/<int:a>/<int:b>` tell Flask?\n   - **a)** Capture a whole number from the URL and call it `a`\n   - **b)** Accept any word, including `three`\n   - **c)** Import the math module automatically\n   - **d)** Send the form with POST\n\n4. Why does `/add/three/five` fail with `<int:a>`?\n   - **a)** `three` and `five` are words, not integers\n   - **b)** Addition only works on the home page\n   - **c)** The browser cannot click links\n   - **d)** Flask does not allow more than one route\n\n5. This lesson's URL calculator puts numbers where?\n   - **a)** In the address bar as part of the URL\n   - **b)** Inside a hidden `static/` folder only\n   - **c)** In the terminal prompt after `(.venv)`\n   - **d)** In a `{% block content %}` in Jinja2\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** Each path gets its own route function in the same `app`.\n2. **a)** `href` is the link destination path.\n3. **a)** `<int:a>` converts the URL piece to an integer named `a`.\n4. **a)** Integer converters reject non-numeric text.\n5. **a)** `/add/3/5` passes numbers through the URL itself.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Как одно Flask-приложение отдаёт Home, About и Jokes?\n   - **a)** Несколько `@app.route` — своя функция на каждый путь\n   - **b)** В файле разрешён только один маршрут\n   - **c)** На каждую страницу нужна отдельная команда `python`\n   - **d)** На каждую страницу нужен свой `.venv`\n\n2. В `<a href='/about'>About</a>` что `href` говорит браузеру?\n   - **a)** Какой путь запросить при клике по ссылке\n   - **b)** Какой цвет CSS использовать\n   - **c)** Какой пакет pip установить\n   - **d)** Секретный ключ для flash-сообщений\n\n3. Что `<int:a>` в `/add/<int:a>/<int:b>` говорит Flask?\n   - **a)** Взять из URL целое число и назвать его `a`\n   - **b)** Принять любое слово, включая `three`\n   - **c)** Автоматически импортировать модуль math\n   - **d)** Отправить форму методом POST\n\n4. Почему `/add/three/five` не работает с `<int:a>`?\n   - **a)** `three` и `five` — слова, а не целые числа\n   - **b)** Сложение только на главной странице\n   - **c)** Браузер не умеет кликать по ссылкам\n   - **d)** Во Flask нельзя больше одного маршрута\n\n5. Где URL-калькулятор этого урока держит числа?\n   - **a)** В адресной строке как часть URL\n   - **b)** Только в скрытой папке `static/`\n   - **c)** В приглашении терминала после `(.venv)`\n   - **d)** В `{% block content %}` в Jinja2\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Каждый путь — своя функция маршрута в одном `app`.\n2. **a)** `href` — путь, куда ведёт ссылка.\n3. **a)** `<int:a>` превращает кусок URL в целое число `a`.\n4. **a)** Конвертер int отклоняет нечисловой текст.\n5. **a)** `/add/3/5` передаёт числа прямо через URL.\n\n</details>\n\n---"
  },
  "lesson-2-1-html-templates": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. Why move HTML into a `templates/` folder instead of one giant string in `app.py`?\n   - **a)** Easier to read, edit, and reuse layout across pages\n   - **b)** Flask refuses to return strings after Block 1\n   - **c)** Browsers cannot show strings anymore\n   - **d)** pip installs templates automatically\n\n2. What does `render_template(\"home.html\", headline=\"Welcome\")` do?\n   - **a)** Fills the template with variables and sends HTML to the browser\n   - **b)** Downloads home.html from the internet\n   - **c)** Copies CSS into the terminal\n   - **d)** Activates the virtual environment\n\n3. In Jinja2, what does `{{ headline }}` mean?\n   - **a)** Show the value of the `headline` variable from Python\n   - **b)** Start a Python function\n   - **c)** Comment out HTML\n   - **d)** Link to `static/style.css`\n\n4. What is `{% block content %}` in `base.html` for?\n   - **a)** A placeholder where each child page puts its unique content\n   - **b)** A list of pip packages\n   - **c)** The Flask secret key\n   - **d)** The port number (5000)\n\n5. A child template starts with `{% extends \"base.html\" %}`. What does that mean?\n   - **a)** Reuse the shared frame (nav, layout) from `base.html`\n   - **b)** Delete `base.html` after saving\n   - **c)** Run the app without `app.run()`\n   - **d)** Send forms with GET only\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** Templates separate page layout from Python logic.\n2. **a)** `render_template` merges data into HTML files.\n3. **a)** Double braces print a variable's value.\n4. **a)** Child templates fill in the `content` block.\n5. **a)** `extends` inherits the parent layout.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Зачем выносить HTML в `templates/`, а не в одну длинную строку в `app.py`?\n   - **a)** Удобнее читать, править и переиспользовать общий каркас\n   - **b)** Flask после Блока 1 не отдаёт строки\n   - **c)** Браузеры больше не показывают строки\n   - **d)** pip сам устанавливает шаблоны\n\n2. Что делает `render_template(\"home.html\", headline=\"Welcome\")`?\n   - **a)** Подставляет переменные в шаблон и отправляет HTML в браузер\n   - **b)** Скачивает home.html из интернета\n   - **c)** Копирует CSS в терминал\n   - **d)** Активирует виртуальное окружение\n\n3. В Jinja2 что значит `{{ headline }}`?\n   - **a)** Показать значение переменной `headline` из Python\n   - **b)** Запустить функцию Python\n   - **c)** Закомментировать HTML\n   - **d)** Ссылка на `static/style.css`\n\n4. Для чего `{% block content %}` в `base.html`?\n   - **a)** Место, куда дочерняя страница вставляет свой уникальный контент\n   - **b)** Список пакетов pip\n   - **c)** Секретный ключ Flask\n   - **d)** Номер порта (5000)\n\n5. Дочерний шаблон начинается с `{% extends \"base.html\" %}`. Это значит?\n   - **a)** Переиспользовать общую рамку (меню, разметку) из `base.html`\n   - **b)** Удалить `base.html` после сохранения\n   - **c)** Запускать приложение без `app.run()`\n   - **d)** Отправлять формы только через GET\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Шаблоны отделяют вёрстку от логики Python.\n2. **a)** `render_template` подставляет данные в HTML-файлы.\n3. **a)** Двойные фигурные скобки выводят значение переменной.\n4. **a)** Дочерние шаблоны заполняют блок `content`.\n5. **a)** `extends` наследует каркас родителя.\n\n</details>\n\n---"
  },
  "lesson-2-2-html-forms": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. With a **GET** form on `/greet`, where does the name often appear after submit?\n   - **a)** In the URL, like `?name=Alex`\n   - **b)** Only inside the server hard drive, never visible\n   - **c)** In the CSS file\n   - **d)** In the `.venv` folder name\n\n2. Which Flask code reads GET data from the URL?\n   - **a)** `request.args.get(\"name\")`\n   - **b)** `request.form.get(\"name\")`\n   - **c)** `flash(\"name\")`\n   - **d)** `url_for('static', filename='name')`\n\n3. Which Flask code reads POST data from a submitted form?\n   - **a)** `request.form.get(\"text\")`\n   - **b)** `request.args.get(\"text\")`\n   - **c)** `importlib.metadata.version(\"text\")`\n   - **d)** `@app.route(\"<text>\")`\n\n4. Why might `/message` use `methods=[\"GET\", \"POST\"]`?\n   - **a)** GET shows the form; POST handles the submitted data on the same URL\n   - **b)** Because GET and POST cannot use templates\n   - **c)** To install Flask twice\n   - **d)** So the URL always shows `?text=...`\n\n5. Why call `.strip()` on form values?\n   - **a)** Remove extra spaces at the start or end of what the user typed\n   - **b)** Delete the form from HTML\n   - **c)** Convert numbers to floats automatically\n   - **d)** Hide flash messages\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** GET form data shows up in the query string.\n2. **a)** `request.args` is for URL query parameters.\n3. **a)** `request.form` is for POST body fields.\n4. **a)** One URL can show the form (GET) and process it (POST).\n5. **a)** `.strip()` trims accidental spaces from user input.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. При **GET**-форме на `/greet` где часто появляется имя после отправки?\n   - **a)** В URL, например `?name=Alex`\n   - **b)** Только на диске сервера, нигде не видно\n   - **c)** В CSS-файле\n   - **d)** В имени папки `.venv`\n\n2. Какой код Flask читает GET-данные из URL?\n   - **a)** `request.args.get(\"name\")`\n   - **b)** `request.form.get(\"name\")`\n   - **c)** `flash(\"name\")`\n   - **d)** `url_for('static', filename='name')`\n\n3. Какой код Flask читает POST-данные из отправленной формы?\n   - **a)** `request.form.get(\"text\")`\n   - **b)** `request.args.get(\"text\")`\n   - **c)** `importlib.metadata.version(\"text\")`\n   - **d)** `@app.route(\"<text>\")`\n\n4. Зачем `/message` может иметь `methods=[\"GET\", \"POST\"]`?\n   - **a)** GET показывает форму; POST обрабатывает данные на том же URL\n   - **b)** Потому что GET и POST не работают с шаблонами\n   - **c)** Чтобы установить Flask дважды\n   - **d)** Чтобы в URL всегда было `?text=...`\n\n5. Зачем вызывать `.strip()` у значений формы?\n   - **a)** Убрать лишние пробелы в начале и конце ввода\n   - **b)** Удалить форму из HTML\n   - **c)** Автоматически превратить числа в float\n   - **d)** Скрыть flash-сообщения\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Данные GET-формы видны в query string.\n2. **a)** `request.args` — для параметров в URL.\n3. **a)** `request.form` — для полей POST.\n4. **a)** Один URL показывает форму (GET) и обрабатывает её (POST).\n5. **a)** `.strip()` убирает случайные пробелы во вводе.\n\n</details>\n\n---"
  },
  "lesson-2-3-madlibs-capstone": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. Where should you copy the starter for your capstone project?\n   - **a)** `my_web_madlibs/` at the project root\n   - **b)** Inside `.venv/Scripts/`\n   - **c)** Only in the browser bookmarks bar\n   - **d)** Into `course-1-python-basics/`\n\n2. The Mad-Libs form uses `method=\"post\"`. Where do the words go when you submit?\n   - **a)** In the request body — read with `request.form.get(...)`\n   - **b)** In the URL as `?noun=robot` only\n   - **c)** Into `static/style.css`\n   - **d)** Directly into `pip list`\n\n3. When should you build the `story` variable?\n   - **a)** Inside the `if request.method == \"POST\":` block after reading the fields\n   - **b)** Before creating `app = Flask(__name__)`\n   - **c)** Only in the browser console\n   - **d)** After `deactivate`\n\n4. What does `{% if story %}` in the template do?\n   - **a)** Show the story section only when `story` is not empty\n   - **b)** Delete the form after one use\n   - **c)** Install Jinja2 with pip\n   - **d)** Change GET to POST automatically\n\n5. Each `<input name=\"noun\">` must match what in `app.py`?\n   - **a)** The same name in `request.form.get(\"noun\")`\n   - **b)** The Flask port number\n   - **c)** The `secret_key` string\n   - **d)** The `@app.route` decorator name only\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** Capstone folders live at project root, like other `my_*` projects.\n2. **a)** POST sends field data in the body, not the URL.\n3. **a)** Build the story only after you have the submitted words.\n4. **a)** The template shows the result block when `story` has text.\n5. **a)** HTML `name` must match the key in `request.form.get(...)`.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Куда копировать стартовый код для капстоуна?\n   - **a)** В `my_web_madlibs/` в корне проекта\n   - **b)** Внутрь `.venv/Scripts/`\n   - **c)** Только в закладки браузера\n   - **d)** В `course-1-python-basics/`\n\n2. Форма Mad-Libs с `method=\"post\"`. Куда попадают слова при отправке?\n   - **a)** В тело запроса — читаем через `request.form.get(...)`\n   - **b)** Только в URL как `?noun=robot`\n   - **c)** В `static/style.css`\n   - **d)** Сразу в `pip list`\n\n3. Когда собирать переменную `story`?\n   - **a)** В блоке `if request.method == \"POST\":` после чтения полей\n   - **b)** До `app = Flask(__name__)`\n   - **c)** Только в консоли браузера\n   - **d)** После `deactivate`\n\n4. Что делает `{% if story %}` в шаблоне?\n   - **a)** Показывает блок истории только если `story` не пустая\n   - **b)** Удаляет форму после одного использования\n   - **c)** Устанавливает Jinja2 через pip\n   - **d)** Сам меняет GET на POST\n\n5. Каждый `<input name=\"noun\">` должен совпадать с чем в `app.py`?\n   - **a)** С тем же именем в `request.form.get(\"noun\")`\n   - **b)** С номером порта Flask\n   - **c)** Со строкой `secret_key`\n   - **d)** Только с именем декоратора `@app.route`\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Итоговые папки — в корне проекта, как другие `my_*`.\n2. **a)** POST отправляет поля в теле запроса, не в URL.\n3. **a)** История собирается только после получения слов из формы.\n4. **a)** Шаблон показывает результат, когда `story` не пустая.\n5. **a)** Атрибут `name` в HTML = ключ в `request.form.get(...)`.\n\n</details>\n\n---"
  },
  "lesson-2-4-static-files-css": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. What is the main job of **CSS** in this lesson?\n   - **a)** Control colors, fonts, and layout — how the page looks\n   - **b)** Store Python routes\n   - **c)** Replace HTML entirely\n   - **d)** Activate the virtual environment\n\n2. Where should `style.css` live in a Flask project?\n   - **a)** In the `static/` folder next to `app.py`\n   - **b)** Inside `templates/base.html` only as plain text\n   - **c)** In the `.venv` folder\n   - **d)** On a USB drive only\n\n3. Why use `{{ url_for('static', filename='style.css') }}` in a template?\n   - **a)** Flask builds the correct link to the CSS file\n   - **b)** To read POST form data\n   - **c)** To set `app.secret_key`\n   - **d)** To define a dynamic route `<name>`\n\n4. HTML tells the browser what to show. CSS tells it what?\n   - **a)** How it should look\n   - **b)** Which pip packages to install\n   - **c)** Which port to open\n   - **d)** How to run `python app.py`\n\n5. What does a `@media (max-width: 480px)` rule help with?\n   - **a)** Adjust styles on narrow screens (responsive layout)\n   - **b)** Block all CSS from loading\n   - **c)** Convert GET forms to POST\n   - **d)** Delete flash messages\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** CSS styles the page — colors, spacing, fonts.\n2. **a)** Flask serves files from `static/` automatically.\n3. **a)** `url_for` generates the right `/static/style.css` path.\n4. **a)** HTML = structure; CSS = appearance.\n5. **a)** Media queries tweak layout for small screens.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Главная задача **CSS** в этом уроке?\n   - **a)** Цвета, шрифты, вёрстка — как выглядит страница\n   - **b)** Хранение маршрутов Python\n   - **c)** Полная замена HTML\n   - **d)** Активация виртуального окружения\n\n2. Где должен лежать `style.css` во Flask-проекте?\n   - **a)** В папке `static/` рядом с `app.py`\n   - **b)** Только внутри `templates/base.html` как текст\n   - **c)** В папке `.venv`\n   - **d)** Только на USB-накопителе\n\n3. Зачем в шаблоне `{{ url_for('static', filename='style.css') }}`?\n   - **a)** Flask строит правильную ссылку на CSS-файл\n   - **b)** Читать POST-данные формы\n   - **c)** Задать `app.secret_key`\n   - **d)** Описать динамический маршрут `<name>`\n\n4. HTML говорит браузеру *что* показать. CSS говорит *что*?\n   - **a)** *Как* это должно выглядеть\n   - **b)** Какие пакеты pip установить\n   - **c)** Какой порт открыть\n   - **d)** Как запустить `python app.py`\n\n5. Чем помогает правило `@media (max-width: 480px)`?\n   - **a)** Подстраивает стили на узком экране (адаптивная вёрстка)\n   - **b)** Блокирует загрузку всего CSS\n   - **c)** Превращает GET-формы в POST\n   - **d)** Удаляет flash-сообщения\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** CSS оформляет страницу — цвета, отступы, шрифты.\n2. **a)** Flask автоматически отдаёт файлы из `static/`.\n3. **a)** `url_for` генерирует правильный путь `/static/style.css`.\n4. **a)** HTML = структура; CSS = внешний вид.\n5. **a)** Media queries подстраивают вёрстку под маленькие экраны.\n\n</details>\n\n---"
  },
  "lesson-2-5-flash-and-validation": {
    "en": "## Quick Check\n\nPick the best answer for each question. Try without scrolling down first!\n\n1. Why does `flash()` need `app.secret_key`?\n   - **a)** Flash uses sessions locally; the secret key makes that work\n   - **b)** To change the CSS background color\n   - **c)** To replace `request.form`\n   - **d)** Because addition only works with a secret\n\n2. When should you call `flash(\"Please enter both numbers.\")`?\n   - **a)** When a required field is empty after POST\n   - **b)** Every time the server starts\n   - **c)** Before activating `.venv`\n   - **d)** Only when the user visits with GET\n\n3. User types `abc` in a number field. What should happen?\n   - **a)** Show a friendly flash error — not a scary crash\n   - **b)** Silently ignore the form forever\n   - **c)** Delete `calc.html`\n   - **d)** Redirect to Course 3 automatically\n\n4. How is this form calculator different from Block 1's `/add/3/5`?\n   - **a)** Users type numbers in a form instead of putting them in the URL\n   - **b)** It does not use Python\n   - **c)** It cannot show errors\n   - **d)** It only works without templates\n\n5. Where do you loop to **display** flash messages in the template?\n   - **a)** `{% for message in get_flashed_messages() %}`\n   - **b)** `{% extends \"flash.html\" %}` only\n   - **c)** `request.args.get(\"message\")`\n   - **d)** Inside `static/style.css`\n\n---\n\n<details><summary>Click to reveal answers</summary>\n\n1. **a)** Sessions need a secret key to store flash messages safely.\n2. **a)** Flash user-friendly errors when validation fails.\n3. **a)** Catch bad input with `try/except` or checks, then `flash()`.\n4. **a)** Forms hide numbers from the URL and feel more like real apps.\n5. **a)** `get_flashed_messages()` returns messages to show once.\n\n</details>\n\n---",
    "ru": "## Проверь себя\n\nВыбери лучший ответ на каждый вопрос. Сначала попробуй без подсказок!\n\n1. Зачем `flash()` нужен `app.secret_key`?\n   - **a)** Flash использует локальные сессии; секретный ключ это включает\n   - **b)** Чтобы сменить цвет фона в CSS\n   - **c)** Чтобы заменить `request.form`\n   - **d)** Потому что сложение работает только с секретом\n\n2. Когда вызывать `flash(\"Please enter both numbers.\")`?\n   - **a)** Когда обязательное поле пустое после POST\n   - **b)** Каждый раз при старте сервера\n   - **c)** Перед активацией `.venv`\n   - **d)** Только при заходе через GET\n\n3. Пользователь ввёл `abc` в поле числа. Что должно произойти?\n   - **a)** Дружелюбная flash-ошибка — без страшного падения\n   - **b)** Форма молча игнорируется навсегда\n   - **c)** Удаление `calc.html`\n   - **d)** Автоматический переход в Курс 3\n\n4. Чем форм-калькулятор отличается от `/add/3/5` из Блока 1?\n   - **a)** Числа вводят в форму, а не в URL\n   - **b)** Не использует Python\n   - **c)** Не может показывать ошибки\n   - **d)** Работает только без шаблонов\n\n5. Где в шаблоне **показать** flash-сообщения?\n   - **a)** `{% for message in get_flashed_messages() %}`\n   - **b)** Только `{% extends \"flash.html\" %}`\n   - **c)** `request.args.get(\"message\")`\n   - **d)** Внутри `static/style.css`\n\n---\n\n<details><summary>Нажми, чтобы увидеть ответы</summary>\n\n1. **a)** Для сессий нужен секретный ключ, чтобы хранить flash.\n2. **a)** Показывай понятные ошибки при неудачной проверке.\n3. **a)** Лови плохой ввод и вызывай `flash()`, а не падай.\n4. **a)** Формы убирают числа из URL — как в настоящих приложениях.\n5. **a)** `get_flashed_messages()` возвращает сообщения для однократного показа.\n\n</details>\n\n---"
  }
}
